import styled from 'styled-components';

export const StyledForm = styled.div`
    input[type="file"] {
        display: none;
    }
    .custom-file-upload {
        display: inline-block;
        font-size:16px;
        cursor: pointer;
        width:100%
    }
`;